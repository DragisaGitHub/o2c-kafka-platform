server:
  port: 8081

auth:
  jwt:
    secret: "change-me-to-a-long-random-string-at-least-32-chars"

spring:

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: checkout-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      properties:
        enable.auto.commit: false
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  r2dbc:
    url: r2dbc:mysql://localhost:3307/checkout_db?serverZoneId=UTC
    username: root
    password: root

  flyway:
    url: jdbc:mysql://localhost:3307/checkout_db?serverZoneId=UTC
    user: root
    password: root
    locations: classpath:db/migration